{
  "globalHeaders": {
    "Cross-Origin-Opener-Policy": "same-origin-allow-popups"
  },
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/assets/*",
      "/api/*",
      "/auth-popup.html",
      "/*.{css,js,png,jpg,jpeg,gif,svg,ico,woff,woff2,txt,map}"
    ]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://pegportal-api-func-cc-001.azurewebsites.net/*"
    }
  ]
}
``